# Mobile Student Notes

## React Native / Expo

The mobile app was started using Expo with the command:

npx expo start

Pressing "w" opens the app in the browser.

The app communicates with the backend server running locally.